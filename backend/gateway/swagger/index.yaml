version: "1.0"         # <- BẮT BUỘC với swagger-combine
name: Gateway API
description: Tổng hợp các dịch vụ con thành một spec

# Servers là phần OpenAPI chấp nhận, nhưng không bắt buộc ở đây
output: ./combined.yaml

apis:
  - url: ./swagger/auth.yaml
    name: auth
